{{- if .Values.postgresql.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgresql-init-script
  labels:
    app: postgresql
data:
  init.sql: |
    -- Initialize database schema
    -- NOTE:
    -- Orderbook data is stored in ClickHouse (table `market_orderbooks`),
    -- not in Postgres. Postgres is used for metadata tables (e.g., `markets`).
{{- end }}

